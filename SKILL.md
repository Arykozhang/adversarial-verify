# adversarial-verify

将任意项目的系统/方案/代码打包为对抗验证材料，发送给独立的对抗AI进行深度诊断。

## 适用场景

- 系统方案需要外部独立审查
- 知道有改进空间但不确定从哪里入手
- 有对标基准（别人的分数/效果更好）需要找差距
- 关键决策前需要"魔鬼代言人"视角

## 核心原则

1. **对抗AI没有之前对话的记忆**——必须把所有背景、历史、试错过程打包进去
2. **文件必须扁平化**——对抗AI通常只能接收文件不能接收文件夹，所有材料放一个目录
3. **不超过50份文件**——精选最有代表性的材料
4. **任务书驱动**——对抗AI需要明确的攻击方向和交付要求
5. **原始材料+提取文本都要给**——原始文件（PDF/代码）和提取/分析后的版本互补

## 使用方式

用户说"对抗验证"、"找AI评审"、"四轮验证"时调用本Skill。

### Step 1: 确认项目类型和范围

向用户确认：
- 要验证的系统/方案是什么？
- 当前水平（分数/效果指标）？对标水平？
- 已知的问题/瓶颈是什么？
- 有哪些历史迭代（前几轮的改进）？

### Step 2: 按模板组装材料包

使用 `templates/package-structure.md` 作为检查清单，收集以下7类材料：

| 类别 | 必备 | 说明 |
|------|------|------|
| 背景 | 项目描述+评分标准+历史迭代 | 对抗AI没有记忆，必须完整 |
| 代表样本 | 3-5份原始文档+提取文本 | 选最有代表性、最难的那批 |
| 源代码 | 核心算法/逻辑的源文件 | 不是全部代码，是核心路径 |
| 输出样例 | 系统实际运行输出 | 让对抗AI看到"系统实际做了什么" |
| 答案/结果 | 系统的最终输出 | 当前最佳版本 |
| 方法论 | 关键设计决策的说明 | 为什么这么做，试过什么，废弃了什么 |
| 任务书 | 攻击方向+交付要求 | 告诉对抗AI"你要找什么" |

### Step 3: 生成任务书

使用 `templates/task-document.md` 模板，填充项目特定内容。

任务书必须包含：
1. **你要理解什么** — 项目本质、架构、当前水平、对标差距
2. **攻击方向** — 至少3个具体方向（检索/推理/效率/架构/数据等）
3. **交付要求** — 具体产出格式
4. **文件索引** — 每个附件的用途说明

### Step 4: 生成沟通文案

使用 `templates/message-template.md` 模板，生成可以直接粘贴到对抗AI对话框的消息。

### Step 5: 打包和交付

将材料包文件夹路径告知用户。用户上传全部文件到对抗AI对话 + 粘贴沟通文案。

### Step 6: 处理回复

对抗AI的回复收到后，按 `templates/execution-command-template.md` 的格式理解任务：

- P0（阻断性缺陷）→ 立即修复
- P1（重要缺陷）→ 排期修复
- P2（优化项）→ 记录评估

在项目中创建 `对抗验证_第N轮/` 文件夹归档。

### Step 7: 干扰清理（关键步骤）

新代码部署后，**必须扫清所有可能干扰新系统运行的文件**。这一步在实战中反复验证了重要性——残留的旧文件会导致：
- 旧版SystemPrompt被误加载
- 已废弃的Agent脚本与新架构冲突
- `__pycache__` 中的过期字节码导致import错误
- 旧答案CSV混淆当前提交版本

**清理清单**：
- [ ] 旧版Agent/推理脚本（已被新架构取代的）
- [ ] 旧版SystemPrompt / Prompt模板（只保留活跃版本）
- [ ] 旧版批量运行器（依赖已删除模块的）
- [ ] 过期的答案CSV（保留当前最佳+上一版本即可）
- [ ] `__pycache__/` 目录（Python字节码缓存）
- [ ] 引用已废弃组件的配置文件
- [ ] 旧版上下文/索引文件（被新版本取代的）

**验证**：清理后运行一遍全流程，确认零import错误、零路径引用错误。

## 使用场景（虚构示例）

> 以下场景展示完整工作流，所有数据和名称均为虚构。

### 场景：代码审查Agent的瓶颈突破

小张在做「智能Code Review Agent」——自动审查GitHub PR并给出修改建议。系统架构是：

```
PR diff → 规则引擎(grep检查命名/格式) → 提取可疑片段 → LLM审查 → 输出Review意见
```

当前指标：
- 召回率 78%（100个真实bug能找出78个）
- 误报率 35%（100个Review意见中有35个是误报）
- 有开源项目宣称做到 92% 召回 + 12% 误报

小张已经做了三轮迭代：
- R1: 纯LLM → 65%召回（基线）
- R2: +规则引擎预筛 → 72%召回（发现LLM被大量无关代码淹没）
- R3: +few-shot示例 → 78%召回（误报从45%降到35%）

现在卡住了，不知道剩下那 14% 召回差距从哪里找。于是启动第四轮对抗验证。

### 小张的操作过程

**Step 1**: 在项目根目录敲 `/adversarial-verify`。Skill加载，确认项目类型、当前水平、对标。

**Step 2**: Skill按模板组装材料包 `对抗验证_第四轮/4.1/`：
- 00: 完整背景（三轮迭代历史、已废弃的AST方案、已知的Java泛型盲区）
- 01-03: 项目描述、评分公式（F1兼顾召回+误报）、高难度case标注（多文件联动修改、泛型擦除）
- 04-08: 5个代表性PR的原始diff和提取后的关键片段
- 09-11: 核心源文件——规则引擎的`rule_engine.py`、LLM调度的`agent.py`、Prompt模板`review_prompt.md`
- 12: 系统Review所用的Prompt和判断规则
- 13-14: 当前最佳输出（100个真实PR的Review结果CSV）和一个token优化版
- 15: 10个关键case的完整链路（PR diff → 规则引擎提取了什么 → LLM输出了什么 → 为什么对/错）
- 16: 已知错误案例（泛型场景漏报、多文件关联修改被截断、Lombok生成代码误报）
- 17: 核心方法论（为什么选了规则+LLM混合而不是纯embedding、Prompt压缩策略）
- 18: 三版迭代的详细变化和每次提了多少分
- 19: 全部200个测试PR的数据集
- 20: 对抗AI任务书（4个攻击方向）

**Step 3**: Skill生成的沟通文案，小张粘贴到对抗AI对话框，上传全部29个文件。

**Step 4**: 对抗AI回复了「第四轮_全面执行命令.md」：发现17个缺陷（9 P0 + 6 P1 + 2 P2）。

关键发现：P0-1规则引擎的泛型参数正则`<T extends \w+>`只匹配单层，漏掉了`<T extends Comparable<T>>`嵌套泛型——导致约6%的漏报！P0-3：Prompt里要求LLM检查空指针，但从来没把变量类型信息喂进去，LLM在瞎猜。

**Step 5**: 小张逐条执行修复，重新跑评测 → 召回率从78%升到87%。

**Step 6**: 修复完成后，小张做了干扰清理——删除旧的`ast_reviewer.py`（被新规则引擎取代）、清空`__pycache__`、归档过期CSV。如果跳过这步，Python会因旧模块残留而import报错。

虽然还没到92%，但找到了最大的两个盲区。下一轮继续。

### 这个场景展示了什么

- **为什么对抗AI需要完整历史**：如果不告诉它"已废弃AST方案"，它可能建议你回去用AST
- **为什么需要系统行为示例（15号文件）**：对抗AI通过对比"规则引擎提取的内容"和"原始PR diff"，一眼就发现泛型被截断了
- **为什么沟通要结构化**：P0/P1/P2分级让小张知道先修哪个，而不是被17个缺陷淹没
- **为什么每个修复配验收标准**：修完就能测，不用再猜"修好了没"

## 文件结构

```
{项目}/对抗验证_第N轮/
├── N.1/                           # 发送给对抗AI的材料（扁平，≤50文件）
│   ├── 00_完整背景_必读.md         # 对抗AI入口文档
│   ├── 01_项目描述.md              # 项目是什么
│   ├── 02_评分标准.md              # 如何衡量好坏
│   ├── 03_高难度样本标注.md        # 哪里容易出错
│   ├── 04-08_代表文档/代码          # 5份最有代表性的材料
│   ├── 09-11_核心源文件             # 系统核心逻辑
│   ├── 12_推理/决策逻辑             # 系统如何做判断
│   ├── 13-14_输出样例+答案          # 系统实际产出
│   ├── 15_系统行为示例              # 关键case的输入输出
│   ├── 16_已知错误与修正.md         # 之前的失败和修复
│   ├── 17_方法论说明.md             # 关键设计+Token/效率方法论
│   ├── 18_迭代历史.md              # 版本演进和分数变化
│   ├── 19_全部样本/题目             # 完整数据集
│   └── 20_对抗AI任务书.md          # 攻击方向+交付要求
└── N.1回复/                       # 对抗AI的回复（后续填充）
    ├── 第四轮_全面执行命令.md       # P0/P1/P2分级执行清单
    ├── plan.md                     # 分析计划
    ├── 方向A_xxx分析报告.md
    ├── 方向B_xxx分析报告.md
    ├── 方向C_xxx分析报告.md
    ├── 方向D_xxx分析报告.md
    └── 第四轮_综合诊断报告.md
```
